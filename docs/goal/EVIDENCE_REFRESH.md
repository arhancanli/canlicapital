# Evidence refresh — 2026-09-20

The website branch now carries the complete newer public export captured from
`/Users/arhancanli/meridian/public`. Production was not changed. The previous
September15 snapshot remains in Git history.

The four publication subtrees and paper-state file were captured together with
before/copy/after hash agreement:1,585files,107,925,058bytes. There were41changed,
19added and0removed paths. Seven added paths were agent state: nested
`.claude-flow` caches and `ruvector.db` files. They were excluded without reading
or publishing their contents. No manuscript files changed.

The engine snapshot overlay had been copying nested agent state despite excluding
it from its source hash. PR68 commit ce93d8c fixes both the ordinary copy and the
fresh-export overlay. Thirteen tests pass, including four new Bash/Zsh and
with/without-design combinations across both site snapshots. Ruff passes. This
change has not been merged or applied to the running publisher.

An isolated candidate used actual repository history and shared installed
libraries. Its first checks failed because an archive had no Git history, then
because shallow history could not prove sitemap date differences; retaining full
history resolved those test-environment failures. Agent files were removed before
the successful rebuilt candidate. The built tree contains none of those paths.

After candidate validation,53changed/added publication files were imported into
the website branch. Six are also generated by site builders; capture and final
hashes are distinguished in `artifacts/algo/evidence-refresh-20260920.json`.
Generated HTML, API summaries, evidence contracts and conformance vectors were
rebuilt from the refreshed inputs.

Validation:

- Candidate and website build/verify pass:6 preliminary plus381 main tests and
  all final audits. Static indexable inventory remains327.
- Chromium and WebKit at320/390/1440 verify the founder snapshot date, five-return
  count, reporting window and absence of horizontal overflow.
- Reproduction verifies163content hashes and2signatures. Engine golden-master
  determinism is not part of this website check.
- Engine transparency verifier checks all1,060entries and signatures. The older
  953entries remain structurally unchanged at the chain prefix.
  There are624disclosed payloads;436historical opaque commitments are not
  independently recoverable payloads.

Current source: forward report generated2026-09-20T06:27:06.093802+00:00,
SHA8e7bad2ae40a1376dbe7a5ac7462689710d0597b7229134b16b3620ecf5e1276.
Five returns, four sleeves, IMMATURE_RECORD_TOO_SHORT. Neither this refresh nor
signature verification establishes Sharpe>2,14qualified sleeves, future drawdown,
actual search indexing, outside adoption or independent replication.

## Deployed-source refresh — 2026-09-20

Retrieved the authenticated Vercel source archive for production deployment
dpl_6xdeHvXKGwt9Nx5Bxsz8DAnyENdq. Its2,851files differ from recorded commit
894ec07d in52public-data files only; application files match the recorded commit.
The candidate already descends from that commit.28glassbox/paper-state files
needed refresh; no nested agent state was present in that scope.

Imported exact deployed source bytes, then regenerated dependent pages/contracts
and vectors. Receipt distinguishes source and final hashes:
artifacts/algo/deployed-evidence-refresh-20260920.json. Full build/verify passed.
163content hashes and2commitment signatures verify; all1,063chain entries verify,
with the prior1,060-entry prefix unchanged.627payloads disclosed,436opaque.
No engine determinism or independent investment-result reproduction claim.
Indexable inventory remains327; no candidate production deployment performed.
