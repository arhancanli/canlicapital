# Phase 14 — approved Figma handoff and isolated preview

Approved 10 September 2026. Production promotion is excluded.

1. Recheck the Figma workflow prerequisite. Stop canvas writes if mandatory instructions remain incomplete; do not retry in a loop or call metadata access a successful design sync.
2. Verify the linked Vercel project and preview-only credential identity. Preserve all earlier deployment evidence and unrelated working-tree changes. No Git push or project-wide environment changes.
3. Deploy the Phase 13 code as a new preview using the existing isolated Supabase store. No new keys, receipts, signups, emails, purchases or trading writes.
4. Check deployment readiness, hosted API health, revised pages, normal-motion navigation, poster failure states and actual Safari where available. Record failures honestly and fix only in-scope regressions.
5. Publish the preview URL, verification coverage and remaining Figma/visual-approval limits. Ask before the next phase.

Prerequisite result: Figma's required gotchas resource still returns a literal 1,791-token truncation gap. No alternative paginated skill reader is exposed. Canvas synchronization is blocked; preview work can proceed independently under the approval.
