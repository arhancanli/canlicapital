# Distinct scenes, connected reading

## Composition correction after review

Reject the process's sticky half-width artwork and the two-column API workbench.
The process becomes one full-viewport optical environment: huge titles travel
across it while the camera pushes through the separated layers. Copy overlays
the scene, never occupies a separate sidebar. The developer film fills the entire
section width; the real API example follows as a full-width foreground console.
Mobile, short screens and reduced motion retain an unpinned reading sequence.

Process: [ full-bleed optical scene / travelling title / lower caption ]
API:     [ full-bleed motion + title ] → [ full-width working example ]

Keep the existing carbon/paper/cobalt/copper palette and type roles below. The
signature is the change in camera scale between research, execution and the
published product—not a repeated illustration next to a column of text.

Subject: systematic research and developer validation, for readers inspecting the
actual paper record. Preserve every existing statement, source, control and URL.

The repeating full-page sidebar was consistent but monotonous. Replace the middle
with distinct compositions: a large exploded optical process study; four wide
strategy rows with individual optical layers; a bright full-width console; a
cobalt developer workbench with original motion footage; a product demonstration
gallery; a vertical decision trace; an editorial research archive; full dated
evidence; a named-builder section; quiet FAQ; a spacious access scene.

Palette stays carbon #000000, paper #f4f4f4, ink #111111, cobalt #0016cb,
copper #ff5500, silver #bac3ce. Chakra Petch / Inter / IBM Plex Mono retain
their display, reading and data roles. The signature is changing scale and
composition around the same optical material, not an identical sidebar everywhere.

Transitions: an expanding aperture from strategies into the light record and a
split-panel handoff into the API. These are decorative, never masks over evidence
or controls. No scroll hijacking. Static/reduced-motion fallbacks keep all content.
An original native 1080p optical film supplies actual moving imagery, separate
from the existing source-bound product demonstration films.

Rejected: extra heading reveals alone, repeating one background for every section,
fake product footage or metrics, unlicensed reference assets, and autoplay sound.

## Media provenance

The archive image was generated with the built-in OpenAI image tool, inspected,
and saved as artifacts/production/optical-archive-v1.png plus its delivery WebP
at public/cinema/optical-archive-v1.webp. Native
output is 1672 by 941; no 4K/upscale claim. Prompt: a wide, photorealistic optical
archive of thick precision-cut clear glass sheets with spherical cut-outs, a
fine continuous copper filament, a low grazing viewpoint, dark negative space,
cobalt lower rim lights, silver reflections and copper highlights; no text,
logos, people, interfaces, numbers or trading claims. Intended as a panoramic
material landscape, not a centered orb or a product screenshot.

The film is a native 1920 by 1080 Blender render, 96 frames at 24fps, from the
existing original optical-master geometry. It is conceptual artwork. Existing
source-bound system films remain distinct. The Higgsfield command was unavailable;
no Higgsfield job, new account, subscription or paid API setup was performed.

## Verified full-scene revision

Removed the split process and API composition. The process now has one desktop
pin owner, full-viewport artwork, three travelling chapters and a changing camera
scale. The developer film fills its environment; the actual API example is a
separate full-width cobalt console. Native film delivery is approximately 2.1 MB.

Fixed two real regressions found during review: image-triggered ScrollTrigger
refresh resetting the external layer pose, and Safari/WebKit's native focus
scroll overriding the selected chapter. Also fixed a wrapped API heading and
keyboard-inaccessible horizontal code overflow at mobile width.

Evidence: artifacts/qa/full-scenes contains rendered scene screenshots, a silent
screen recording and review.html. Chromium/WebKit tests cover three distinct
poses, full-width geometry, chapter focus, video play/pause/offscreen behavior
and reduced-motion teardown. Actual Safari covers the home journey and 12 routes.
Content-preservation check: 489 pages unchanged in main text, IDs, links and
controls. Build and 269 application tests plus two cache/prose regression tests
pass. Automated accessibility checks: 14 views, zero violation groups. These are
checks, not accessibility certification or a claim of production readiness.

No deployment, publishing-checkout overwrite, Figma sync, marketing launch,
real API-key generation or backend write testing was performed in this revision.
