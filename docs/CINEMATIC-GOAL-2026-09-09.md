# Cinematic redesign goal

Status: active, not visual parity and not a production-release certification.

## Ground truth

The user rejected the latest lower-chapter pass as too basic. Do not treat larger
headings, paper cards, or three camera crops as the final design. The reference
uses a persistent subject that moves through changing environments and connects
the sections. The next production work must address that, not repeat isolated
CSS treatments.

Figma file: https://www.figma.com/design/n3MbdBAC6STjHudQocaZa7

An early metadata-only inspection incorrectly suggested that the file contained
only a cover. Full page enumeration corrected this: foundations, Action and
Question components, and a desktop homepage composition already exist. Preserve
them. Existing desktop frame: 46:2 on page 0:1. Existing action set: 35:2.

New revision page: 91:30, "Cinematic revision / 09 Sep".
Editable scene studies: 92:2, 92:11, 92:20. Native Smart Animate click transitions
connect their action instances 92:8, 92:17, 92:26. They reuse the actual Canli
fonts, original optical artwork and Action component. These are exploratory
camera compositions, not the completed website or true 3D animation.

The Safari-imported evidence snapshot 89:2 is incomplete: some animated content
was omitted. Do not cite it as a complete design or source of product truth.

## Current implementation checkpoint

- Completed the interrupted lower-chapters stylesheet and controller.
- Full-width evidence stage; explicit desktop state selection; static fallback.
- Accessible film tabs; malformed hash handling; original transcripts retained.
- Five-step provenance route; valid list markup; scroll carrier.
- Larger research covers, separate observed/objective registers, black colophon,
  cobalt FAQ. The user still considers this level insufficient.
- Fixed duplicate renderer RAF creation, reduced-motion state persistence,
  shader alpha compositing and reduced-motion skip-link positioning.
- Kept original HTML text, links, IDs and form controls across 489 tracked pages.

## Remaining design work

1. Build an original continuous research-packet journey with meaningful spatial
   handoffs, environment changes and deliberately paced camera holds. Keep dense
   evidence reachable outside the cinematic path.
2. Art-direct complete desktop and mobile homepage compositions in Figma using
   the existing foundations and components; prototype key transitions and states.
3. Review and redesign representative subpage families: strategies, developers,
   research/library, article, performance/evidence, tools and methodology. Apply
   shared improvements through generators, not manual edits to hundreds of files.
4. Production motion assets must earn their scale and resolution. Reuse existing
   original render sources where suitable; do not stretch small bitmap details
   and claim new native resolution.
5. Test browser behavior, API onboarding, responsive hierarchy, accessibility,
   loading/failure/reduced-motion states, budgets and source preservation. Do not
   submit real API-key, signup or investment flows during read-only QA.
6. Compare actual browser recordings against the reference's chronological
   contact sheets. Record shortcomings, not just test counts. No deployment until
   the intended production target and release authority are clear.

## Efficiency checks

At each chapter milestone: screenshot, explicit critique, fix or document the
remaining mismatch, then move on. Avoid repeating connection checks or dumping
whole Figma trees. Fetch exact nodes and return bounded summaries. Do not change
baselines to make tests pass. Preserve the original publishing checkout and
unrelated user changes.

## Connected decision chapter checkpoint

The previous goal turn made progress, not a wait: implemented and tested lower
sections and authored three editable Figma camera studies.

The next turn replaced the desktop decision-route sidebar with a full-viewport
five-stage carrying scene. Existing five list items/links remain intact; the
scenery and optical packet live on separate layers, with fixed reading holds,
explicit stage navigation and keyboard focus jumps. Text occlusion found during
the first screenshot review was corrected. Narrow/short viewports and reduced
motion retain the complete vertical sequence; save-data keeps a poster.

Files: js/decision-journey.js and css/decision-journey.css. QA:
scripts/audit-decision-journey.py and expanded actual-Safari audit. Art remains
conceptual and explicitly labelled, not an actual order replay. No new financial
numbers or state claims are invented.

Remaining critique: the environments are vector stage studies, not the reference's
fully rendered environments. Mobile retains the earlier reading layout rather
than a newly art-directed cinematic composition. Figma's three preliminary
camera studies do not yet represent these five implemented stages. These gaps
remain in scope; passing interactions is not a visual-quality sign-off.

The frozen-boundary stage now uses an original OpenAI-generated glass/rail
environment, saved as public/cinema/decision-boundaries-v1.webp (143 KB, native
1672 x 941). See DECISION-ARTWORK-2026-09-09.md for the prompt and provenance.
The other stages still need further art direction. Five-stage motion, direct
stage controls, reverse jumps and unobscured evidence links passed actual Safari;
Chromium/WebKit also passed keyboard focus and reduced-motion teardown/re-entry.
