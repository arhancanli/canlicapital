.vercel
node_modules
dist
*.log
.DS_Store

# Playwright/render-audit output — regenerated on every run, never source.
test-results/

# Phase snapshots kept locally while the redesign landed. History lives in git now.
.bak/
.claude/
