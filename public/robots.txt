User-agent: *
Allow: /
Disallow: /company-data/

# Every page here is meant to be readable by people and by machines. Nothing is
# gated, nothing is cloaked, and no crawler is singled out for different content.
# /company-data/ holds the raw JSON and SEC snapshots behind each company page. They
# stay public and linked for people, the API and the MCP servers; search crawlers
# skip them because every one is served noindex and the pages that cite them are
# what a search should find.

Sitemap: https://canlicapital.com/sitemap.xml

# An LLM-oriented map of the site, in the llmstxt.org convention. It leads with the
# claim boundary -- paper-traded, not funded -- because a summary that gets that
# wrong is the most costly error a model can make about this project.
# https://canlicapital.com/llms.txt
#
# The same corpus in full, in one file, for models that would rather read than
# crawl: every research document, including every failed one.
# https://canlicapital.com/llms-full.txt
